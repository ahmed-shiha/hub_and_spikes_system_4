
name: Monitor Production FWA Model
on:
  schedule:
    - cron: '0 9 * * *' # Run at 9 AM UTC every day
  workflow_dispatch:
jobs:
  check-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python & Install Dependencies
        run: |
          pip install -r ml_hub/fwa_detection_model_v1/requirements.txt
          pip install -e ml_hub/medins_ml_utils --no-deps
      - name: Run Production Drift Check
        working-directory: ./ml_hub/fwa_detection_model_v1
        run: |
          # In a real system, this would download the latest prediction logs first
          python monitoring/check_production_drift.py
